# Generate Sexton tree cover tile names for a region

Generate Sexton tree cover tile names for a region

## Usage

``` r
sexton_tile_names(roi)
```

## Arguments

- roi:

  sf object, SpatVector, or numeric bbox

## Value

Character vector of tile names in format "N00W060"
