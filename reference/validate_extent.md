# Validate and standardize extent input

Validate and standardize extent input

## Usage

``` r
validate_extent(extent)
```

## Arguments

- extent:

  Either an sf object or a numeric vector of length 4 (xmin, ymin, xmax,
  ymax)

## Value

A numeric vector of length 4 with bbox coordinates
