# Generate Hansen GFC tile names for a region

Generate Hansen GFC tile names for a region

## Usage

``` r
hansen_tile_names(roi)
```

## Arguments

- roi:

  sf object, SpatVector, or numeric bbox

## Value

Character vector of tile names in format "10N_080W"
